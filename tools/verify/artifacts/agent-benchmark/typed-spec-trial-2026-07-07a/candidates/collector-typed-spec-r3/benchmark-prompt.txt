You are running one ThreeNative typed-spec benchmark session.

Condition-specific starter instructions:
- Work only in this candidate project directory.
- Use the generated ThreeNative workflow in AGENTS.md and CLAUDE.md.
- This is the typed-spec condition: make durable authoring changes in src/game.spec.ts first, then compile it with `pnpm tn -- authoring compile-typed-spec --project . --json` before normal verification. Do not use bare `tn`; this isolated candidate may not have node_modules installed.
- For game work, start before source mutation with `pnpm tn -- game plan --goal "collector benchmark: visible character, five pickups, score/progress, win state, retry path" --project . --json`.
- After gameplay/input/source changes, verify with the generated project loop. Prefer `pnpm tn -- iterate --project . --json` when available.
- Stop immediately after you have a playable browser game matching the benchmark prompt and the relevant local verification passes. Do not perform extra manual/browser checks after a passing verification loop. Do not edit files outside this candidate project.

Benchmark prompt:
Build a small browser game where the player controls a visible character with WASD or arrow keys, collects at least five visible pickups, and sees a score or progress indicator update. The objective is to collect all pickups. Include a clear win state and a retry path.

Visual bar: the player, pickups, floor or arena boundary, and score UI must be readable without inspecting code.
