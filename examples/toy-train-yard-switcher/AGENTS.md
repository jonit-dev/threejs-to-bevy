# AGENTS.md

This example follows the repo and `examples/AGENTS.md` rules.

- Durable source lives under `content/**/*.json` and `src/scripts/**/*.ts`.
- `dist/**` and `artifacts/**` are generated proof outputs.
- Keep gameplay in `src/scripts/player.ts`; structured source references the exported system.
- Preserve asset sourcing and fallback notes when changing visual surfaces.
