# AGENTS.md

Rules for this generated ThreeNative example.

- Durable source lives in `content/**/*.json` and `src/scripts/**/*.ts`.
- `dist/` and `artifacts/` are generated evidence; do not hand-edit them.
- Keep visual and gameplay changes source-authored through structured source
  and scripts.
