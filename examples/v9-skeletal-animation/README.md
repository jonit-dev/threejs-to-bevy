# V9 Skeletal Animation

Focused proof scene for cross-runtime glTF skeletal animation deformation.

- Loads bundle-local `assets/hero.glb` (Khronos Fox sample model)
- Plays the `Run` clip on load
- Uses a static camera framing the animated character

Verification:

```bash
pnpm verify:v9:skeletal-animation
```
