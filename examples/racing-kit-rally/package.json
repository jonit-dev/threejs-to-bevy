{
  "name": "threenative-racing-kit-rally",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tn build",
    "dev:web": "tn dev --target web",
    "validate": "tn validate",
    "validate:authoring": "tn authoring validate --json",
    "verify": "tn verify --frames 4 --expect-motion --json"
  },
  "dependencies": {
    "@threenative/sdk": "workspace:*"
  },
  "devDependencies": {
    "@threenative/cli": "workspace:*",
    "@types/node": "^20.19.25",
    "typescript": "^5.9.3"
  }
}
