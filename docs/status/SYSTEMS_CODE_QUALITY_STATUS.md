# Systems Code Quality Status

Living log for systemic code-quality, architecture, and technical-debt status.
Update it when a new system is introduced or when an agent finds the code in a
systemically bad place. Keep entries brief.

Last updated: 2026-07-13

## Snapshot

- Overall status: ⚠️ **Needs focused hardening**
- ⚠️ Baseline quality score: **7.2/10** (2026-07-08). A fresh numeric rerating
  is pending after the July 9 remediation and parity gates accumulate
  cross-hardware history.
- Source taxonomy: `docs/bevy-feature-parity.md`
- ✅ Systems code-quality remediation closed the previous top four red rows:
  IR contract truth, game-loop scheduling, native live reconciliation, and
  compiler bundle planning/writer separation.
- ✅ Adapter-surface remediation closed all five planned slices: config-owned
  generated-game enrollment, drift gates, typed CLI registry plumbing,
  executable authoring descriptors, and editor metadata/composite recipes.
- ✅ The leverage-point bundle closed all seven slices: adapter derivation,
  churn ratchets, runtime diagnostics, gate descriptors, manifest ownership,
  the build-only mid-size forcing function, and bundle-backed visual metrics.
- ✅ The feature-parity-polishing bundle closed shared residuals, calibrated
  visual proof, native UI/text/accessibility, physics/navigation depth, and
  audio/platform runtime policy with focused gates.
- ✅ Gameplay parity now has state/timing probes, coverage-debt checks,
  negative controls, web/desktop comparison reports, and release enrollment.
- ⚠️ Primary risk theme: contract truth and runtime behavior are still split
  across SDK, IR, compiler, web runtime, Bevy runtime, editor/CLI adapters, and
  verification registries.
- ✅ The 2026-07-13 deep physics audit remediation closed all P0 findings and
  most P1/P2 findings across compiler emission, live Rapier, scripting, and
  validation. Conservative script queries and proof-vs-live convergence remain
  tracked hardening work in the audit addendum.
- ⚠️ Rust static analysis is documented but not enforced: `pnpm check:rust`
  is absent, pre-push omits Clippy/rustfmt, and a 2026-07-13 Clippy baseline
  found 91 native-runtime library warnings before an all-targets overlay-test
  compile error stopped analysis. A shrinking lint/path ratchet is planned.
- Deep diagnostic for the previous top four 🔴 rows (now closed):
  `docs/status/systems-code-quality-diagnostic-2026-07-08.md`.
- Deep diagnostic for the adapter-surface rows closed on 2026-07-08:
  `docs/status/systems-code-quality-diagnostic-adapter-surfaces-2026-07-08.md`.
- ✅ The 2026-07-09 engine bug/performance audit closed all 16 confirmed
  correctness, parity, lifecycle, and algorithmic findings. The remaining
  watch item is cross-hardware dense-physics and browser GPU-memory history
  before setting tighter release budgets.
- ✅ Completed adapter-surface remediation bundle:
  `docs/PRDs/other/adapter-surface-remediation-2026-07-08/README.md`.
- ❌ Current measured product failure: off-recipe checkpoint-race remains
  3.614x vanilla raw tokens and physics-knockdown remains 2.008x against the
  <=2x gate; fresh Round 5B reruns have not yet cleared that claim.
- ⚠️ `examples/neon-harbor-rescue` is a valuable mid-size forcing function but
  remains build-only pending production art, durable persistence, interactive
  settings proof, and visual/release evidence.

Legend: ✅ completed accomplishment, ⚠️ current warning, ❌ measured failure;
table status uses 🔴 urgent systemic risk, 🟡 watch or harden next, and 🟢
acceptable with normal maintenance.

## System Map

Urgent items stay first. Add or revise rows when a new system lands, a claim is
promoted, or code quality is clearly in a bad place.

| Status | System | Current quality risk | Next action | Evidence |
| --- | --- | --- | --- | --- |
| 🟡 | Rust static analysis and code-smell policy | Rustfmt and Clippy are named in architecture docs, but the root `check:rust` command is missing and pre-push static checks cover TypeScript only. The 2026-07-13 baseline reported 91 `threenative_runtime` library warnings and could not finish all targets because the active overlay test/module surface did not compile together. | Restore a complete all-targets baseline, add one workspace-owned lint policy and structured lint/path debt ratchet, run it before expensive pre-push phases, then shrink debt to zero and enforce `-D warnings`; do not hide debt with blanket lint allows. | `docs/PRDs/other/rust-static-analysis-quality-ratchet-2026-07-13.md`; `package.json`; `scripts/verify-pre-push.mjs`; `runtime-bevy/Cargo.toml`; `scripts/check-source-size.mjs` |
| 🟡 | Physics, collision, and character movement | The 2026-07-13 audit remediation closed all P0 findings plus joints, gravity, same-tick body commands, cache lifecycle/wake behavior, sensors, character filters, mesh bounds, layer capacity, and SDK/IR drift. Remaining systemic risk is duplicated conservative query/proof geometry instead of retained live Rapier observations, plus long-running dense-world hardware history. | Move script query and proof claims onto normalized live Rapier observations or add a comprehensive differential matrix; retain the 100/1,000-body benchmark and add 5,000-body hardware samples before tightening release budgets. | `docs/audits/PHYSICS_SYSTEM_AUDIT_2026-07-13.md`; `packages/runtime-web-three/src/physics.test.ts`; `packages/runtime-web-three/src/systems/services/physics.test.ts`; `runtime-bevy/crates/threenative_runtime/tests/physics.rs`; `runtime-bevy/crates/threenative_runtime/tests/systems_services.rs`; `runtime-bevy/crates/threenative_runtime/tests/systems_host.rs` |
| 🟡 | Rendering, materials, lights, cameras, and post-processing | Live reconciliation, cubemaps, visibility, stable cascades, contact shadows, bounded volumetrics, temporal web SSGI, spatial native SSGI, deterministic baked SH2 probes, collider-independent tooling/native scene-ray queries, layers, and teardown are implemented. The hero-interior forcing function exposed systemic leaks now fixed: Bevy's implicit `AmbientLight(80)` contaminated probe composition, both conformance adapters assumed `level`/`pause` lifecycle IDs, native-only grading multipliers clipped blue from dark mids, coupled homogeneous fog could not reproduce independent height falloff and shafts, and native SSGI was only a flat ambient lift. Native height fog now owns an HDR depth-aware post pass; Bevy's shadowed volumetric raymarch is independently calibrated; native SSGI uses a stable depth-weighted spatial neighborhood gather that restores smooth wall/floor color bleed without temporal fireflies or coherent ray curtains. Forward native rendering maps baked SH2 into bounded irradiance volumes. Bevy 0.14 deferred lighting has a shader-definition defect for irradiance volumes, so SSR/deferred scenes retain an honestly reported SH-L0 atmosphere baseline plus spatial SSGI instead of compiling a black material pipeline. The composed gate guards clipping, crushed blacks, feature application, cross-adapter exposure/contrast, rendered shaft contrast/luminance, height haze, bloom halos, chroma, and surface detail without requiring native high-frequency grain. Temporal SSGI invalidation, volumetric/SSGI shader cost, the deferred irradiance limitation, probe hash coverage, scene-query rebuild policy for live topology changes, cascade sampling, and depth-aware SSR remain adapter-private drift risks. | Keep the hero showcase, volumetrics, SSGI, baked-GI, bloom-anchor, and grey-ramp gates green without weakening regional/noise bands or re-coupling height density to shaft radiance. Keep cascade stability, contact-shadow lifecycle/cost, SSGI scheduling/history/orbit proof, baked-probe determinism/staleness/warm-lift proof, render-geometry query tests, shared profile reporting, the full photoreal color matrix, and render-resource teardown green. | `examples/lumen-lite-showcase`; `tools/verify/src/lightingShowcaseGate.ts`; `runtime-bevy/crates/threenative_runtime/src/height_fog_postprocess.rs`; `runtime-bevy/crates/threenative_runtime/src/height_fog_postprocess.wgsl`; `runtime-bevy/crates/threenative_runtime/src/ssgi_postprocess.rs`; `runtime-bevy/crates/threenative_runtime/src/ssgi_postprocess.wgsl`; `packages/compiler/src/bake/sceneRayQuery.test.ts`; `runtime-bevy/crates/threenative_runtime/src/scene_ray_query.rs`; `packages/runtime-web-three/src/rendering/directionalShadowController.test.ts`; `packages/runtime-web-three/src/rendering/contactShadows.test.ts`; `packages/runtime-web-three/src/rendering/godrays/GodRaysPass.test.ts`; `packages/runtime-web-three/src/rendering/ssgi/temporalResolvePass.test.ts`; `packages/runtime-web-three/src/rendering/bakedProbeLighting.test.ts`; `tools/verify/src/volumetricsGate.ts`; `tools/verify/src/ssgiGate.ts`; `tools/verify/src/bakedGiGate.ts`; `runtime-bevy/crates/threenative_runtime/tests/rendering_atmosphere.rs`; `runtime-bevy/crates/threenative_runtime/tests/rendering.rs` |
| 🟡 | IR/source/runtime contract truth | Contract truth is still broad, but high-risk drift now has typed registry metadata, optional-field checks, compiler literal checks, enum drift checks, and schemas for systems/gameFlow/prefabs. | Keep remaining unschemed documents as incremental follow-up slices; run IR drift tests before schema/DTO/compiler changes. | `docs/PRDs/done/other/system-code-quality-remediation-2026-07-08/PRD-003-ir-document-contract-truth-hardening.md`; `packages/ir/src/contractDrift.test.ts`; `packages/ir/schemas/systems.schema.json`; `pnpm --filter @threenative/ir test` |
| 🟡 | Structured authoring ownership and selector lowering | Event/resource inference, compact-instance tags, tag/pattern command expansion, sibling system/UI ownership, and reference-aware removal now cross authoring validation, SDK world snapshots, compiler emission, and CLI registries. These are safer than parallel adapters but remain a drift risk while the same ownership contract is represented in multiple document families. | Keep selector expansion fail-closed, derive CLI/help surfaces from registries, and retain focused tests for sibling-document cascades, event schema inference, and emitted selector commands. | `packages/authoring/src/operations/scene.ts`; `packages/compiler/src/emit/ecs.ts`; `packages/compiler/src/scripts/resourceAccess.ts`; `packages/cli/src/mechanicBlocks/registry.ts`; `packages/compiler/src/emit/ecs.test.ts` |
| 🟡 | Native game loop scheduling | Startup, fixed-step accumulator, pause, interpolation, frame, tick, and update/postUpdate ordering now share fixture-backed expectations across web and native. A 2026-07-13 overlay playtest exposed paused web render frames consuming pending input before gameplay resumed; paused frames now preserve pending input and a focused regression guards the resume boundary. | Keep new scheduling behavior tied to `loop-scheduling/expectations.json`; preserve the paused-input regression and broaden only through shared fixture snapshots. | `docs/PRDs/done/other/system-code-quality-remediation-2026-07-08/PRD-002-native-web-game-loop-scheduling-contract.md`; `packages/ir/fixtures/contracts/loop-scheduling/expectations.json`; `packages/runtime-web-three/src/gameLoop.test.ts`; `runtime-bevy/crates/threenative_runtime/tests/game_loop_contract.rs` |
| 🟡 | Native scripted spawn/despawn reconciliation | Native script spawn/despawn/instantiate effects now reconcile bundle mutations into live Bevy ECS entities, hierarchy, recursive despawn, and collider teardown proof. | Keep command/effect traces honest: live reconciliation evidence must accompany future script structural command claims. | `docs/PRDs/done/other/system-code-quality-remediation-2026-07-08/PRD-001-native-scripted-spawn-despawn-live-reconciliation.md`; `runtime-bevy/crates/threenative_runtime/src/lib.rs`; `runtime-bevy/crates/threenative_runtime/tests/systems_effects.rs`; `cargo test -p threenative_runtime --lib scripted_runtime_should --manifest-path runtime-bevy/Cargo.toml` |
| 🟡 | Compiler bundle emission | Bundle planning, asset dependency discovery, and staged filesystem writing are split, with direct unit coverage for planning, writer behavior, asset copy planning, merges, and structured readers. | Keep `emitBundle` routed through planner/writer seams; add planner assertions for new document/capability outputs before changing writers. | `docs/PRDs/done/other/system-code-quality-remediation-2026-07-08/PRD-004-compiler-bundle-planning-writer-split.md`; `packages/compiler/src/emit/bundle.ts`; `packages/compiler/src/emit/bundle-writer.ts`; `packages/compiler/src/emit/merge.test.ts`; `pnpm --filter @threenative/compiler test -- --run bundle` |
| 🟡 | PlacementSet source expansion | Scene-owned PlacementSets expand before bundle emission into ordinary prefab instances with deterministic IDs and source provenance. Validation, compiler expansion, CLI migration, and durable-source ownership are separate seams, so merge/path and registry drift remain the main risks. | Keep expansion deterministic and fail closed on ID/path collisions; require exact-match dry runs before removing explicit source and keep descriptor/adapter drift coverage for every placement operation. | `packages/authoring/src/placementSets.ts`; `packages/authoring/src/operations/placement.ts`; `packages/compiler/src/authoring/placementSets.ts`; `packages/cli/src/commands/scene.ts`; `tools/verify/src/adapterSurfaceDrift.test.ts`; `pnpm --filter @threenative/authoring test -- --run placement` |
| 🟡 | Registry-owned document defaults | Exact archetype, GameFlow, and Sequence copies can use compact preset references expanded by the authoring loader. This centralizes truth but makes loader expansion a validation/compiler boundary. | Keep the vocabulary closed, preserve ordinary expanded IR, and require adoption drift tests plus explicit local source whenever semantics differ. | `packages/authoring/src/documentPresets.ts`; `tools/verify/src/adoptionRatchet.test.ts`; `docs/cookbook/registry-owned-document-defaults.md` |
| 🟡 | Interaction fixed-tick executors | Web and native consume one closed Interaction IR and now match normalized pickup, hazard, checkpoint, and projectile traces plus resulting resource/entity state. The executors remain separate implementations and therefore retain ordering, effect-service, lifecycle, and write-ledger drift risk. | Keep detector/gate/effect vocabulary closed; enroll each new effect in paired trace/state fixtures and negative controls before promotion, and preserve the 512-trace bound and explicit runtime diagnostics. | `packages/ir/src/interactions.ts`; `packages/runtime-web-three/src/interactions.ts`; `runtime-bevy/crates/threenative_runtime/src/interactions.rs`; `tools/verify/src/interactionParity.ts`; `packages/ir/artifacts/conformance/interactions/`; `pnpm verify:conformance` |
| 🟡 | Authoring operations and mutation surfaces | Operation truth is still broad, but migrated scene/material/runtime/UI operations carry executable CLI adapter metadata, and editor composites/add-component payloads now route through one metadata layer over authoring descriptors. | Continue shrinking explicit adapter-surface drift allowlists as remaining command/editor surfaces gain descriptors. | `docs/PRDs/done/other/adapter-surface-remediation-2026-07-08/PRD-002-adapter-surface-drift-gates.md`; `docs/PRDs/done/other/adapter-surface-remediation-2026-07-08/PRD-004-executable-authoring-operation-descriptors.md`; `docs/PRDs/done/other/adapter-surface-remediation-2026-07-08/PRD-005-editor-operation-metadata-and-composite-recipes.md`; `tools/verify/src/adapterSurfaceDrift.test.ts`; `packages/authoring/src/operationRegistry.ts`; `packages/editor/src/operations/editorOperationMetadata.ts`; `pnpm --filter @threenative/authoring test`; `pnpm verify:editor-required-operations` |
| 🟢 | Plan-emitted authoring command integrity | Plan recipe flags/cookbook IDs previously drifted, recipe retries could leave partial state, and additive archetypes could steal authored camera ownership. Plans now derive from descriptors, recipes stage before commit and adopt project inventory, and a cross-template/archetype gate measures every emitted command. Fresh scaffold proof failures are accepted only when the game-quality gate confirms empty systems or missing mutation proof. | Keep `emittedCommandFailureRate` at zero unexpected failures; enroll new emitted surfaces in the owning descriptor and gate. | `tools/agent-benchmark/HANDS-ON-AUTHORING-AUDIT-2026-07-09.md`; `tools/verify/src/emittedCommandGate.ts`; `packages/authoring/src/recipes.ts`; `packages/cli/src/commands/game.ts`; `pnpm verify:emitted-commands` |
| 🟡 | CLI command surface | `tn` command metadata, help, and migrated dispatch now route through an incremental typed command registry, and source-document hot spots can derive CLI usage/argv from executable authoring operation descriptors. Some large command families still own local argv parsing. | Continue migrating remaining command families behind descriptors or explicit drift diagnostics. | `docs/PRDs/done/other/adapter-surface-remediation-2026-07-08/PRD-003-cli-command-registry-and-shared-arg-plumbing.md`; `docs/PRDs/done/other/adapter-surface-remediation-2026-07-08/PRD-004-executable-authoring-operation-descriptors.md`; `packages/cli/src/commands/registry.ts`; `packages/cli/src/commands/sourceDocuments.ts`; `packages/cli/src/index.test.ts`; `pnpm --filter @threenative/cli test`; manual smoke: `tn actor list --json`, `tn build --project templates/structured-source-starter --json`, `tn proof diff --from /tmp/tn-proof-a.json --to /tmp/tn-proof-b.json --json` |
| 🟡 | Editor source operations | Editor operation metadata now decorates authoring descriptors, owns add-component payload builders, and defines composite recipes used by both store plans and server execution; required-operation smoke covers default-scene and terrain composites. | Keep new editor operations descriptor-backed or add explicit metadata and smoke coverage with the operation. | `docs/PRDs/done/other/adapter-surface-remediation-2026-07-08/PRD-005-editor-operation-metadata-and-composite-recipes.md`; `packages/editor/src/operations/editorOperationMetadata.ts`; `packages/editor/src/adapters/editorModel.test.ts`; `packages/editor/src/state/editorStore.ts`; `packages/editor/src/server/operationApi.ts`; `tools/verify/src/editorRequiredOperations.ts`; `pnpm --filter @threenative/editor test`; `pnpm verify:editor-required-operations` |
| 🟡 | Generated-game verification | Release enrollment and proof requirements come from project-local `production.releaseProof` config, example lifecycle policy comes from `examples/manifest.json`, and visual-quality proof now requires a reusable `game-quality` metric bundle that is checked against screenshot metrics. Generated-game scoring now also rejects registered empty gameplay exports and missing resource/HUD mutation scenarios; cookbook and emitted-command gates validate the cross-reference and proof semantics that feed those reports. | Keep generated-game release/build-only enrollment derived from config and examples manifest before promoting additional examples; keep new proof sidecars bundle-backed instead of adding one-off JSON checks, and preserve explicit native/visual blockers for build-only forcing functions such as Coin Patrol. | `docs/PRDs/PRD-authoring-golden-path-frictions-2026-07-11.md`; `packages/authoring/src/gameWorkflow.ts`; `tools/verify/src/cookbookGate.ts`; `tools/verify/src/emittedCommandGate.ts`; `tools/verify/src/gameProductionGate.ts`; `examples/coin-patrol`; `examples/manifest.json`; `pnpm verify:cookbook`; `pnpm verify:emitted-commands` |
| 🟡 | Scripting host and services | Web and Bevy separately reconstruct context services, commands, resources, observations, and host bridges. A 2026-07-13 chess failure exposed that Bevy's service-to-readable-component mapping omitted the implicit `Camera`/`Transform` dependency of `picking.pointerRay`; the focused service test now guards it, but the hand-maintained mapping remains a drift risk. | Keep service-by-service parity fixtures for animation, audio, physics, picking, UI, persistence, resources, lifecycle, and gameplay primitives; require each service test to work without redundant explicit component reads, and move implicit data dependencies into the owning service descriptor when one exists. | `packages/runtime-web-three/src/systems`, `packages/runtime-web-three/src/patrol.ts`, `runtime-bevy/crates/threenative_runtime/src/systems_context.rs`, `runtime-bevy/crates/threenative_runtime/tests/systems_services.rs`, `runtime-bevy/crates/threenative_runtime/src/patrol.rs`, `runtime-bevy/crates/threenative_runtime/src/systems_host_bridge.rs`, `docs/status/capabilities/scripting.md`, `pnpm verify:gameplay-primitives` |
| 🟡 | Animation, particles, and VFX | Promoted subset is bounded, but graph/blend/morph/mask/particle semantics span SDK/IR/runtime adapters and residual gates. | Add small conformance fixtures before expanding graph, morph, mask, property animation, or particle behavior. | `packages/sdk/src/animation.ts`, `packages/ir/src/animation-residuals.test.ts`, `runtime-bevy/crates/threenative_runtime/src/animation.rs`, `docs/bevy-feature-parity.md` |
| 🟡 | UI, text, widgets, and accessibility | Web DOM overlay and Bevy UI have separate layout, widgets, input, binding, accessibility, font, and style semantics. Several native UI rows remain trace/metadata only. | Maintain widget-family conformance fixtures and negative tests for unsupported layout/style semantics before expanding native UI. | `packages/runtime-web-three/src/ui`, `runtime-bevy/crates/threenative_runtime/src/ui.rs`, `docs/status/capabilities/ui.md` |
| 🟡 | Native CEF overlay ownership | The transparent GTK/Wry path failed NVIDIA/Xwayland pixel correctness and has been removed. Linux now renders each CEF surface off-screen into a bounded latest-frame Bevy texture under one CEF session, serves only bundle-local assets, rejects stale resize generations, retries snapshots without sequence skips, and routes pointer, wheel, keyboard, focus, live pointer islands, authored bounds/stable z-order, and modal suppression. Screenshot readiness requires a nonblank paint from every surface, and resize plus minimize/restore lifecycle pixels are gated. A descriptor-owned manifest drives hash-checked, overlay-only AppImage packaging; the release probe passed 300 transitions with a flat five-second process-tree RSS settle window. Full IME, other compositors, and Windows/macOS remain open. | Keep claims Linux- and evidence-bounded, keep CEF updates manifest/checksum-reviewed, and add platform evidence before enabling another target. | `runtime-bevy/crates/threenative_runtime/src/overlay_cef`; `runtime-bevy/cef-runtime-manifest.json`; `packages/cli/src/commands/package.ts`; `tools/verify/src/nativeOverlayCefGate.ts`; `tools/verify/artifacts/native-overlay-cef/verification-report.json` |
| 🟡 | Assets, glTF, scenes, terrain, and streaming | Asset source, manifest, inspection, dependency bundling, glTF fidelity, terrain generation, and runtime loading span many packages and gates. | Keep asset claims manifest-backed; add drift tests when adding formats, loaders, glTF extension handling, terrain, or streaming policy. | `docs/status/capabilities/assets.md`, `packages/ir/src/assetValidation.ts`, `packages/compiler/src/emit`, `runtime-bevy/crates/threenative_runtime/src/assets.rs` |
| 🟡 | Audio | The native scheduling gap is closed for event one-shots, script play/stop, and feedback-preset audio, with registry-owned headless execution proof; authored pause/resume/stop/query controls are sink-backed. Web and Bevy still use different sinks and state bridges; script query feedback, native seek, audible device output, generated waveforms, custom decoders, and streaming remain bounded. | Keep the native event/script execution cases in the focused audio gate, retain normalized trace comparison for metadata/policy, and require explicit diagnostics for unsupported sink/backend behavior. | `packages/runtime-web-three/src/audio.ts`; `runtime-bevy/crates/threenative_runtime/src/audio.rs`; `runtime-bevy/crates/threenative_runtime/tests/audio.rs`; `tools/verify/src/audioPlatform.ts`; `docs/status/capabilities/audio-platform.md` |
| 🟡 | Input, picking, and controls | Keyboard/pointer/gamepad/touch/UI action semantics span source docs, runtime snapshots, Bevy input capture, playtests, and UI routing. | Add targeted fixtures when changing canonical key codes, pointer deltas, touch/gamepad gestures, picking, or UI action routing. | `packages/runtime-web-three/src/input.ts`, `runtime-bevy/crates/threenative_runtime/src/input.rs`, `docs/bevy-feature-parity.md` |
| 🟡 | Camera, transforms, math, and geometry | Core transform semantics are stable, but helpers, interpolation, generated meshes, terrain, render targets, and follow/orbit behavior cross runtime adapters. | Keep transform-patch and helper semantics covered by shared fixtures; isolate geometry generation from runtime mapping. | `packages/runtime-web-three/src/cameras.ts`, `runtime-bevy/crates/threenative_runtime/src/cameras.rs`, `docs/bevy-feature-parity.md` |
| 🟡 | Persistence, settings, and local data | Save/settings behavior is promoted, but storage migration, autosave/checkpoint hooks, and cloud/account boundaries remain sensitive. | Require fixture coverage for migration/versioning and restore flow before adding new persisted surfaces. | `packages/sdk/src/persistence.ts`, `packages/ir/src/localDataValidation.ts`, `docs/bevy-feature-parity.md` |
| 🟡 | GameFlow, sequences, spawners, and declarative gameplay | Promoted traces exist, but this is a growing gameplay layer over source, compiler, runtime, and conformance contracts. | Keep new trigger/action/track kinds fail-closed until a shared fixed-tick trace fixture proves them. | `docs/status/capabilities/game-production.md`, `runtime-bevy/crates/threenative_runtime/src/game_flow.rs`, `runtime-bevy/crates/threenative_runtime/src/sequences.rs`, `runtime-bevy/crates/threenative_runtime/src/spawner.rs` |
| 🟡 | Playtest, proof, and performance tooling | Verification is disciplined, but orchestration files are broad. Agent IO, session-cost, and webview-package gates now derive focused dispatch and release artifact enrollment from gate descriptors, with reviewed migration gaps for remaining inline focused gates. | Keep migrating release/focused gates behind descriptors before adding new release constants, and model conflict policies in descriptor data. | `packages/cli/src/commands/playtest.ts`, `tools/verify/src/gateDescriptors.ts`, `tools/verify/src/cli/run.ts`, `tools/verify/src/release.ts`, `docs/status/capabilities/tooling-proof.md`; `pnpm --filter @threenative/verify-tools test` |
| 🟡 | Templates and starters | Maintained starter generated files, instruction files, package scripts, and proof command ids are owned by `threenative.template.json`, and `verify:template-production` derives script/instruction/API-card/proof checks from those manifests. CLI template registry truth remains a separate follow-up surface. | Keep new starter policy in template manifests and add a registry drift check before expanding starter families. | `templates/*/threenative.template.json`, `packages/cli/src/templates/registry.ts`, `templates/*/package.json`, `templates/*/AGENTS.md`, `tools/verify/src/templateProductionGate.ts`; `pnpm verify:template-production` |
| 🟡 | Examples and benchmark projects | Examples have manifest-owned lifecycle classification, and release/build-only generated-game gates derive from `examples/manifest.json` plus project config. The new build-only `examples/neon-harbor-rescue` mid-size forcing function exposes follow-up gaps for catalog art, full local-data persistence, and interactive settings proof before release enrollment. Dependency, proof, and artifact strategies still differ by release, build-only, and benchmark-only role. | Continue deriving build/proof gates from `examples/manifest.json` before adding new example roles or promotions; convert repeated Neon Harbor friction into focused implementation PRDs. | `examples/manifest.json`, `examples/neon-harbor-rescue/FRICTION.md`, `tools/verify/src/exampleManifest.ts`, `tools/verify/src/gameProductionGate.ts`, `tools/verify/src/exampleBuildSweep.ts` |
| 🟡 | Docs/status claims | Capability pages, parity doc, STATUS front door, release gates, and this log can drift from executable registries. | Link each promoted system claim to one owning registry/gate artifact instead of duplicating capability truth in prose. | `docs/STATUS.md`, `docs/bevy-feature-parity.md`, `docs/status/capabilities/*.md` |
| 🟡 | Script context typing drift | Runtime services not yet typed on public `ScriptContext` are guarded by an explicit migration allowlist; picking moved to shared IR types in PRD-001 phase 3. | Remove allowlist entries as public service contracts move to shared types; do not let new runtime services enter silently. | `packages/script-stdlib/src/index.test.ts`, `packages/script-stdlib/src/script-context.ts`, `packages/runtime-web-three/src/systems/contextTypes.ts` |
| 🟢 | Public SDK physics collider cleanup | The SDK/IR cylinder collider mismatch is mitigated; renderable cylinder mesh remains distinct from portable physics colliders. | Watch for downstream migration needs after `cylinderCollider()` removal. | `docs/status/code-quality-audit-2026-07-04.md`, `packages/sdk/src/physics.ts`, `packages/ir/src/physics.test.ts` |
| 🟢 | Verification gate discipline | Focused gates, artifact policy, release aggregation, and tests exist. Main risk is registry sprawl, not absence of proof culture. | Keep gates focused and descriptor-backed as new systems are added. | `tools/verify/src/*.test.ts`, `tools/verify/src/release.ts`, `package.json` |

## Verification Expectations

- Runtime contract changes: run the narrow package tests plus
  `pnpm verify:conformance`.
- Native runtime changes: include relevant `cargo test` coverage under
  `runtime-bevy`.
- Compiler or IR emission changes: snapshot emitted files and manifest shape,
  then run compiler and IR tests.
- Authoring, CLI, editor, template, or proof-gate changes: run the narrow
  package tests plus the relevant focused verify gate.
- Docs-only status updates: no verification required beyond checking links and
  Markdown readability.

## Update Rules

- Add or revise an entry when introducing a new runtime, compiler, authoring,
  verification, editor, asset, platform, or gameplay system.
- Add or revise an entry when local work reveals systemic fragility, duplicated
  contract truth, hard-to-test orchestration, unsafe boundaries, or sustained
  technical debt.
- Use 🔴, 🟡, or 🟢 and keep 🔴 rows at the top.
- Prefer short dated edits over long audit prose.
- Move items to 🟢 only when the behavior is changed and verification is
  recorded.
- Link the deeper audit, PRD, capability page, or test artifact instead of
  copying details here.
