{
  "schema": "threenative.project",
  "version": "0.1.0",
  "entry": "content/scenes/arena.scene.json",
  "outDir": "dist/structured-source-starter.bundle",
  "production": {
    "playableLoop": "Move the player across the arena to reach the goal feedback.",
    "controls": ["keyboard.KeyA", "keyboard.ArrowLeft", "keyboard.KeyD", "keyboard.ArrowRight"],
    "objective": "Trigger visible and audio feedback by reaching the goal zone.",
    "progression": "The starter remains a single objective slice; expand it with score, timer, waves, or collectible count before claiming a complete generated game.",
    "failRetry": "Reset by rebuilding or reloading the preview; no persistent fail state is authored in this starter.",
    "feedbackMoments": [
      "Player movement is visible in playtest proof.",
      "Goal feedback changes scene state when the player reaches the objective.",
      "HUD text can be extended from GameState bindings."
    ],
    "assetPlan": {
      "playerHero": "Starter player entity is source-owned placeholder geometry intended to be replaced by catalog, kit, or authored custom art.",
      "obstacleEnemy": "No obstacle/enemy is authored in the starter baseline.",
      "rewardInteractable": "Goal zone is the starter reward/interactable surface.",
      "worldEnvironment": "Arena floor, camera, light, and goal context provide the starter world surface.",
      "uiHud": "Starter HUD/source UI is the baseline for objective and state text.",
      "audioFeedback": "Starter goal-ping audio asset is available for feedback wiring."
    },
    "scriptModules": [
      {
        "module": "src/scripts/player.ts",
        "export": "updatePlayer",
        "ownsState": ["Transform", "GameState"],
        "referencedBy": ["content/systems/arena.systems.json"]
      }
    ],
    "proofCommands": [
      "tn authoring validate --project . --json",
      "tn build --project . --json",
      "tn playtest --project . --entity player --press keyboard.KeyD --frames 30 --expect-moved --json",
      "tn screenshot --project . --url <preview-url> --out artifacts/game-production/screenshot.png --wait-ready --json",
      "tn game score --project . --json",
      "tn game qa --project . --run-proof --json",
      "tn game release --project . --json"
    ]
  },
  "template": "structured-source-starter"
}
