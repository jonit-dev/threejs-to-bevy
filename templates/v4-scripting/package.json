{
  "name": "threenative-v4-scripting-game",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tn build",
    "dev:web": "tn dev --target web",
    "test": "pnpm build && tsc -p tsconfig.test.json && node --test dist/tests/gameplay.test.js",
    "validate": "tn validate dist/v4-scripting.bundle",
    "verify": "tn verify --frames 3 --expect-motion --json"
  },
  "dependencies": {
    "@threenative/sdk": "workspace:*"
  },
  "devDependencies": {
    "@threenative/cli": "workspace:*",
    "@types/node": "^20.19.25",
    "typescript": "^5.9.3"
  }
}
