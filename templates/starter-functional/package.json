{
  "name": "starter-functional",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tn build",
    "validate": "tn validate",
    "verify": "tn verify --frames 2 --json",
    "package:desktop": "tn package --bundle dist/starter-functional.bundle --target desktop --out artifacts/package --json"
  },
  "dependencies": {
    "@threenative/sdk": "workspace:*",
    "@threenative/ui": "workspace:*"
  }
}
